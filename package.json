{
  "name": "deskchat",
  "version": "0.1.0",
  "description": "A local desktop AI chat with screenshots, image upload, mouse control, and terminal tools.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "lint": "npm run typecheck",
    "build": "npm run typecheck && npm test",
    "build:app": "electron .",
    "typecheck": "node --check main.js && node --check preload.js && node --check renderer.js && node --check settings.js && node --check quick-add-parser.js && node --check quick-add-parser.selftest.js && node --check goal-decomposer.js && node --check goal-decomposer.selftest.js && node --check goal-task-detector.js && node --check goal-task-detector.selftest.js && node --check todo.js && node --check todo-system.selftest.js && node --check main-todo-store.selftest.js && node --check todo.e2e.js",
    "test": "npm run test:quick-add && npm run test:goal-decomposer && npm run test:goal-task-detector && npm run test:todo-system && npm run test:main-todo-store",
    "test:quick-add": "node quick-add-parser.selftest.js",
    "test:goal-decomposer": "node goal-decomposer.selftest.js",
    "test:goal-task-detector": "node goal-task-detector.selftest.js",
    "test:todo-system": "node todo-system.selftest.js",
    "test:main-todo-store": "node main-todo-store.selftest.js",
    "test:e2e": "node todo.e2e.js"
  },
  "devDependencies": {
    "electron": "^31.7.7",
    "playwright-core": "^1.60.0"
  },
  "dependencies": {
    "@highlightjs/cdn-assets": "^11.11.1",
    "d3": "^7.9.0",
    "dompurify": "^3.4.8",
    "highlight.js": "^11.11.1",
    "katex": "^0.17.0",
    "mammoth": "^1.12.0",
    "marked": "^18.0.4",
    "markmap-lib": "^0.18.12",
    "markmap-view": "^0.18.12",
    "pdf-parse": "^2.4.5",
    "xlsx": "^0.18.5"
  }
}
